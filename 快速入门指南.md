# 🚀 快速入门指南

在 5 分钟内完成 Rust WASM 图片压缩模块的集成，性能提升 **20-40%**！

---

## 📋 前置要求

- ✅ **Node.js** v18+
- ✅ **Git**
- ✅ **Rust** (安装脚本会自动处理)
- ✅ **wasm-pack** (安装脚本会自动处理)

---

## 🔧 5 分钟快速集成

### 步骤 1：克隆项目

```bash
git clone https://github.com/fjqz177/pic-smaller.git
cd pic-smaller
```

### 步骤 2：一键安装与构建

**Windows PowerShell**:

```powershell
# 安装项目依赖
npm install

# 安装 Rust (如果还没有)
# 访问 https://rustup.rs/ 下载并安装

# 添加 WASM 目标
rustup target add wasm32-unknown-unknown

# 安装 wasm-pack
cargo install wasm-pack

# 构建 WASM 并集成到项目
npm run wasm:full
```

**Linux/macOS**:

```bash
# 安装项目依赖
npm install

# 安装 Rust (如果还没有)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env

# 添加 WASM 目标
rustup target add wasm32-unknown-unknown

# 安装 wasm-pack
cargo install wasm-pack

# 构建 WASM 并集成到项目
npm run wasm:full
```

### 步骤 3：启动测试

```bash
npm run dev
```

访问 http://localhost:3000 测试压缩功能。

---

## 📊 性能对比

| 格式         | 旧 WASM | 新 Rust WASM | 提升       |
| ------------ | ------- | ------------ | ---------- |
| PNG (1080p)  | ~2.0s   | **~1.2s**    | ⬆️ **40%** |
| AVIF (1080p) | ~3.5s   | **~2.8s**    | ⬆️ **20%** |

_测试环境：1920x1080 图片，中等质量设置_

---

## 🎯 常用命令

| 命令                     | 说明                   |
| ------------------------ | ---------------------- |
| `npm run dev`            | 启动开发服务器         |
| `npm run build`          | 生产构建               |
| `npm run preview`        | 预览生产构建           |
| `npm run test`           | 运行测试               |
| `npm run lint`           | 代码检查               |
| `npm run format`         | 代码格式化             |
| `npm run clean`          | 清理构建文件           |
| `npm run clean:all`      | 清理所有（包括 WASM）  |
| `npm run wasm:build`     | 仅构建 WASM 模块       |
| `npm run wasm:integrate` | 集成 WASM 到项目       |
| `npm run wasm:full`      | 一键完成所有 WASM 操作 |

---

## ✅ 检查清单

- [ ] `public/wasm/` 目录包含以下文件：
  - `pic_compress_wasm_bg.wasm` - WASM 二进制文件
  - `pic_compress_wasm.js` - JavaScript 胶水代码
  - `pic_compress_wasm.d.ts` - TypeScript 类型定义
- [ ] `src/engines/PicCompressWasm.ts` 存在（TypeScript 包装器）
- [ ] `vite.config.ts` 包含 COOP/COEP headers
- [ ] 开发服务器正常启动
- [ ] 图片压缩功能正常工作

---

## 🏗️ 项目结构

```
pic-smaller/
├── pic-compress-wasm/          # Rust 源代码
│   ├── src/
│   │   ├── lib.rs              # WASM 入口
│   │   ├── png.rs              # PNG 压缩
│   │   ├── avif.rs             # AVIF 压缩
│   │   ├── error.rs            # 错误处理
│   │   └── utils.rs            # 工具函数
│   ├── Cargo.toml              # Rust 配置
│   ├── Cargo.lock              # 依赖锁定文件
│   ├── rust-toolchain.toml     # Rust 工具链配置
│   └── pkg/                    # 编译输出（自动生成）
├── public/
│   ├── logo.svg                # 项目 Logo
│   └── wasm/                   # WASM 文件（构建后生成）
│       ├── pic_compress_wasm.wasm
│       ├── pic_compress_wasm.js
│       └── pic_compress_wasm.d.ts
├── src/
│   ├── engines/
│   │   ├── PicCompressWasm.ts  # TypeScript 包装器
│   │   ├── PngImage.ts         # PNG 图像处理
│   │   ├── AvifImage.ts        # AVIF 图像处理
│   │   ├── CanvasImage.ts      # Canvas 图像操作
│   │   ├── ImageBase.ts        # 图像基类
│   │   ├── Queue.ts            # 压缩队列管理
│   │   ├── handler.ts          # 事件处理器
│   │   ├── transform.ts        # 图像变换
│   │   ├── support.ts          # 特性检测
│   │   ├── createWorker.ts     # Worker 创建器
│   │   ├── WorkerCompress.ts   # 压缩 Worker 入口
│   │   ├── WorkerPreview.ts    # 预览 Worker 入口
│   │   └── gif.wasm            # GIF 压缩 WASM 模块
│   ├── components/             # React 组件
│   ├── hooks/                  # React Hooks
│   ├── utils/                  # 工具函数
│   ├── types/                  # TypeScript 类型定义
│   └── ...
├── scripts/
│   ├── build-wasm.cjs          # WASM 构建脚本 (CommonJS)
│   ├── build-wasm.bat          # Windows 批处理版本
│   ├── build-wasm.sh           # Unix Shell 版本
│   ├── integrate-wasm.cjs      # WASM 集成脚本
│   ├── integrate-wasm.bat      # Windows 批处理版本
│   ├── integrate-wasm.sh       # Unix Shell 版本
│   └── clean.cjs               # 清理脚本
├── tests/                      # 测试文件
├── docs/                       # 文档
│   └── log_doc/
│       └── WASM_INTEGRATION.md # 详细集成文档
├── .gitignore
├── .eslintrc.cjs
├── .prettierrc
├── tsconfig.json
├── vite.config.ts
├── package.json
└── index.html
```

---

## 🔍 故障排除

### wasm-pack 未安装

**错误**: `wasm-pack not found`

**解决**:

```bash
cargo install wasm-pack
```

或者使用官方安装脚本：

```bash
curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
```

### Rust 未安装

**错误**: `cargo: command not found`

**解决**:

**Windows**: 访问 https://rustup.rs/ 下载安装

**Linux/macOS**:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

### WASM 加载失败

**错误**: `WebAssembly.instantiate() failed`

**解决**:

1. 检查 `vite.config.ts` 中的 headers 配置：
   ```typescript
   server: {
     headers: {
       "Cross-Origin-Opener-Policy": "same-origin",
       "Cross-Origin-Embedder-Policy": "require-corp",
     },
   }
   ```
2. 确保 `public/wasm/` 目录存在且包含 WASM 文件
3. 清除浏览器缓存并刷新

### 构建失败

**错误**: WASM 构建过程中出错

**解决**:

```bash
# 清理 WASM 构建文件
cd pic-compress-wasm
rm -rf pkg target
cd ..

# 重新构建
npm run wasm:full
```

### 类型错误

**错误**: TypeScript 类型不匹配

**解决**:

```bash
# 重新生成类型定义
npm run wasm:build
```

---

## 💡 开发提示

### 开发模式构建

开发模式下构建更快（但文件更大）：

```bash
cd pic-compress-wasm
wasm-pack build --dev --target web --out-dir ../public/wasm
```

### 生产构建优化

生产构建已启用以下优化：

- **LTO (Link-Time Optimization)**: 完整链接时优化
- **opt-level 3**: 最高优化级别
- **strip**: 移除调试信息
- **wasm-opt -O4**: WASM 二进制优化

### 启用 SIMD 加速

在 `pic-compress-wasm/Cargo.toml` 中添加：

```toml
[profile.release]
target-cpu = "simd128"  # 启用 WASM SIMD
```

然后重新构建：

```bash
npm run wasm:build
```

---

## 📖 更多文档

- [`README.md`](./README.md) - 项目总览
- [`docs/log_doc/WASM_INTEGRATION.md`](./docs/log_doc/WASM_INTEGRATION.md) - 详细集成文档
- [`pic-compress-wasm/README.md`](./pic-compress-wasm/README.md) - WASM 模块 API 文档

---

**集成完成！** 🎉
